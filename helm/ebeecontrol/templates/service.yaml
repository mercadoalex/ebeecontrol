apiVersion: v1
kind: Service
metadata:
  name: {{ include "ebeecontrol.fullname" . }}
  labels:
    {{- include "ebeecontrol.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: health
      protocol: TCP
      name: health
  selector:
    {{- include "ebeecontrol.selectorLabels" . | nindent 4 }}
